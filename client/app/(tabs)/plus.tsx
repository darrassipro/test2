import React from 'react';
import { View } from 'react-native';

// Placeholder screen for the + tab. The tabBarButton in the layout
// intercepts presses and opens a modal, so this screen can be empty.
export default function Plus() {
  return <View />;
}
